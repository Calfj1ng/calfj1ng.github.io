# 「每日学习」栏目的访问密码。
# 复制本文件为 .env（.env 已在 .gitignore 中，不会提交）并填入真实密码。
#
# 部署到 GitHub Pages 时，需要在仓库 Settings → Secrets and variables → Actions
# 添加同名 secret：DAILY_PASSWORD。
#
# 提醒：源 markdown 仍在本 public 仓库里，本密码只保护 calfj1ng.github.io
# 上渲染后的内容，挡不住直接读 GitHub 源码的人。真正敏感的内容不要放这里。
DAILY_PASSWORD=change-me-to-a-long-random-string
